enum activeStatus {
    Active
    InActive
}

enum Role {
    user
    admin
    author
}

enum PostStatus {
    DRAFT
    PUBLISHED
}

enum CommentStatus {
    APPROVE
    REJECT
}

enum Subscripionstatus {
    Active
    Cancel
    expired
}
